# --- Core API framework -----------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9

# --- Data layer ---------------------------------------------------------
sqlalchemy>=2.0
# psycopg2-binary>=2.9        # uncomment + set DATABASE_URL for Postgres

# --- Prototype AI / vision -----------------------------------------------
# NOTE: mediapipe>=1.0 requires opencv-contrib-python (not headless).
# Do NOT add opencv-python-headless alongside this — they conflict.
opencv-contrib-python>=4.9
pillow>=10.2
numpy>=1.26
networkx>=3.2

# --- Real face detection & alignment (3.1 Biometrics) --------------------
# MediaPipe ships pre-built wheels for Windows / Linux / macOS (Python 3.9-3.12).
# CPU-only, no CUDA required. Model weights (~32 MB) are downloaded automatically
# the first time the detector is initialised.
mediapipe>=1.0

# --- PDF support for face detection on document uploads ------------------
# pypdfium2: pure-Python PDF rasterizer (no poppler / system libs needed).
# Allows face detection to work when a PDF document (passport scan, ID scan)
# is uploaded instead of a raw JPEG/PNG image.
pypdfium2>=4.20

# --- Testing --------------------------------------------------------------
pytest>=8.1
httpx>=0.27

# ---------------------------------------------------------------------------
# PRODUCTION-GRADE MODEL DEPENDENCIES (installed)
# ---------------------------------------------------------------------------
# These packages are installed in .venv and enable full production-grade
# AI models. Swap in the real service implementations per the "swap-in"
# notes at the top of each services/*.py file.
#
paddleocr>=2.7               # app/services/ocr_service.py -> PaddleOCRService
paddlepaddle>=2.6
torch>=2.2                   # app/services/tampering_service.py / face_service.py
insightface>=0.7.3           # app/services/face_service.py -> InsightFaceService
onnxruntime>=1.17
faiss-cpu>=1.8.0             # app/services/identity_service.py -> FAISS-backed index
qdrant-client>=1.8           # app/services/identity_service.py -> Qdrant-backed index
